<UserControl x:Class="Hidden_Hills.MVVM.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hidden_Hills.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Foreground="Transparent">

    <UserControl.Resources>
        <Style TargetType="{x:Type Border}">
            <Setter Property="Margin" Value="15,0,0,0"></Setter>
        </Style>
    </UserControl.Resources>

    <!-- Home Main View Screen-->
    <StackPanel>
        <TextBlock Text="Let's start!"
                   Foreground="White"
                   FontSize="32"
                   HorizontalAlignment="Left"
                   Margin="2,2,0,8"/>

        <StackPanel Orientation="Horizontal">
            <Border Width="300" Height="200" Margin="2,0,0,0">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,2">
                        <GradientStop Color="DarkSlateBlue" Offset="0.0"/>
                        <GradientStop Color="DarkMagenta" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <Border.Clip>
                    <RectangleGeometry RadiusX="10" RadiusY="10" Rect="0,0,400,200" />
                </Border.Clip>

                <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <!-- Logo i napis -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                        <Image Source="/Images/data-collection.png" Width="64" Height="64" Margin="10"/>
                        <TextBlock Text=" Capture &#x0a; Packets" Foreground="White" FontSize="28" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Przycisk na dole -->
                    <Button Content="Proceed to " Width="300" Height="40" Margin="10,30,10,10"/>
                </StackPanel>

            </Border>
        </StackPanel>

        <TextBlock Text="Most Used Tools ->"
           Foreground="White"
           FontSize="18"
           HorizontalAlignment="Left"
           Margin="2,8,0,8"/>

        <StackPanel Orientation="Horizontal">
            <!-- Pierwszy Border -->
            <Border Width="150" Height="150"
                    Background="HotPink"
                    CornerRadius="10">
                <StackPanel VerticalAlignment="Center" 
                            HorizontalAlignment="Center">
                    <Image Source="/Images/data-collection.png" 
                           Width="64" 
                           Height="64"
                           Margin="0,0,0,10"/>
                    <TextBlock Text="Convert To CSV" 
                               Foreground="White" 
                               FontSize="18"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Drugi Border -->
            <Border Width="150" Height="150"
                    Background="HotPink"
                    CornerRadius="10">
                <StackPanel VerticalAlignment="Center" 
                            HorizontalAlignment="Center">
                    <Image Source="/Images/server.png" 
                           Width="64" 
                           Height="64"
                           Margin="0,0,0,10"/>
                    <TextBlock Text="Encrypt Packets" 
                               Foreground="White" 
                               FontSize="18"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Trzeci Border -->
            <Border Width="150" Height="150"
                    Background="HotPink"
                    CornerRadius="10">
                <StackPanel VerticalAlignment="Center" 
                            HorizontalAlignment="Center">
                    <Image Source="/Images/lock.png" 
                           Width="64" 
                           Height="64"
                           Margin="0,0,0,10"/>
                    <TextBlock Text="Decrypt with KEY" 
                               Foreground="White" 
                               FontSize="18"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </StackPanel>

    </StackPanel>

</UserControl>